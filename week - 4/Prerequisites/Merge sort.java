package weblab;import java.util.*;class Solution {    /**     * @param elements     *     Array of integers to be sorted.     * @return     *     New array of sorted integers.     */    public static int[] mergeSort(int[] elements) {        if (elements == null || elements.length <= 1) return elements;        int[] l = Arrays.copyOfRange(elements, 0 , elements.length/2);        // print(l);        int[] r = Arrays.copyOfRange(elements, elements.length/2, elements.length);        // print(r);        // print(new int[0]);        l = mergeSort(l);        r = mergeSort(r);        int[] toReturn = new int[elements.length];        int i = 0;        int j = 0;        int k = 0;        while (i < l.length && j < r.length) {            if (l[i] < r[j]) toReturn[k++] = l[i++];            else toReturn[k++] = r[j++];        }        while (i < l.length) toReturn[k++] = l[i++];        while (j < r.length) toReturn[k++] = r[j++];        return toReturn;    }    public static void print(int[] arr) {        StringBuilder sb = new StringBuilder();        for (int i = 0; i < arr.length; i++) {            sb.append(arr[i] + ", ");        }        System.out.println(sb);    }}